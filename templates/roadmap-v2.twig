<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Firefly III ecosystem overview</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">

</head>
<body>
<div class="container">
    <nav class="nav navbar bg-light navbar-expand-lg">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">Firefly III</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                    aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse nav-tabs" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item" role="presentation">
                        <button id="home-tab" data-bs-toggle="tab"
                                data-bs-target="#home-tab-pane" type="button" role="tab"
                                aria-controls="home-tab-pane" aria-selected="true"
                                class="nav-link active" aria-current="page">Home
                        </button>
                    </li>
                    {% for category in categories %}
                        <li class="nav-item" role="presentation">
                            <button class="nav-link"
                                    id="{{ category.key }}-tab"
                                    data-bs-toggle="tab" data-bs-target="#{{ category.key }}-tab-pane"
                                    type="button" role="tab" aria-controls="{{ category.key }}-tab-pane"
                                    aria-selected="false">
                                {{ category.title }}</button>
                        </li>
                    {% endfor %}
                </ul>
            </div>
        </div>
    </nav>
    <div class="tab-content" id="myTabContent">
        <div class="tab-pane show active" id="home-tab-pane" role="tabpanel" aria-labelledby="home-tab"
             tabindex="0">
            <h1>Firefly III ecosystem overview</h1>
            <p class="lead">
                {{ intro_text }}
            </p>
        </div>
        {% for category in categories %}
            <div class="tab-pane" id="{{ category.key }}-tab-pane" role="tabpanel"
                 aria-labelledby="{{ category.key }}-tab" tabindex="0">
                <div class="row mb-2">
                    <div class="col">
                        <h1>{{ category.title }}</h1>
                        <p>
                            <a href="https://github.com/{{ category.repos }}" title="Link to GitHub"><img
                                        alt="GitHub Repo stars"
                                        src="https://img.shields.io/github/stars/{{ category.repos }}?style=flat"></a>
                            <a href="https://github.com/{{ category.repos }}/releases"
                               title="Link to GitHub releases"><img alt="GitHub release (latest by date)"
                                                                    src="https://img.shields.io/github/v/release/{{ category.repos }}"></a>
                            <br>
                            <em>{{ category.description }}</em>
                        </p>
                        <ul>
                            {% if category.last_commit_develop %}
                                <li>Last commit to <code>develop</code>
                                    was {{ category.last_commit_develop.last_commit_date }}</li>
                            {% endif %}
                            {% if category.last_commit_main %}
                                <li>Last commit to <code>main</code>
                                    was {{ category.last_commit_main.last_commit_date }} (usually coincides with the
                                    latest release)
                                </li>
                            {% endif %}
                        </ul>
                        <div class="row mb-2">
                            {% for releaseType in releaseTypes %}
                            <div class="col">
                                {% for card in category.info[releaseType] %}
                                    <div class="card">
                                        <div class="card-header">
                                            Next {{ releaseType }} version:
                                            <strong>v{{ card.version }}</strong>
                                        </div>
                                        <div class="card-body">
                                            <ul>
                                                <li>
                                                    <a href="{{ card.url }}">
                                                        {{ card.count }}
                                                        issues open for this version
                                                    </a>
                                                </li>
                                            </ul>
                                        </div>
                                    </div>
                                {% endfor %}
                            </div>
                            {% endfor %}
                        </div>
                        {% if category.projects %}
                            {% for project in category.projects %}
                                <h3>{{ category.title }} - Project "{{ project.title }}"</h3>
                                <p><em>{{ project.description }}</em></p>
                                <p>
                                    TODO how to display this?
                                </p>
                            {% endfor %}
                        {% endif %}
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
    <div>
        <h2>Support</h2>
        <p>
            If you need support using Firefly III or the associated tools, come find us!
        </p>
        <ul>
            <li><a href="https://github.com/orgs/firefly-iii/discussions">GitHub Discussions for questions and
                    support</a>
            </li>
            <li><a href="https://gitter.im/firefly-iii/firefly-iii">Gitter.im for a good chat and a quick answer</a>
            </li>
            <li><a href="https://github.com/firefly-iii/firefly-iii/issues">GitHub Issues for bugs and issues</a></li>
            <li><a href="https://fosstodon.org/@ff3">Follow me around for news and updates on Mastodon</a></li>
            <li><a href="mailto:james@firefly-iii.org">Email me</a></li>
        </ul>
    </div>
</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-OERcA2EqjJCMA+/3y+gxIOqMEjwtxJY7qPCqsdltbNJuaOe923+mo//f6V8Qbsw3"
        crossorigin="anonymous"></script>
{% for i, category in categories %}
    {% for ii, info in category.info %}
        {% if 'ticket-task-chart' == info.type %}
            <script type="module" src="{{ info.hash }}.js"></script>
        {% endif %}
    {% endfor %}
{% endfor %}
<!-- Matomo -->
<script>
    var _paq = window._paq = window._paq || [];
    /* tracker methods like "setCustomDimension" should be called before "trackPageView" */
    _paq.push(['trackPageView']);
    _paq.push(['enableLinkTracking']);
    (function () {
        var u = "https://analytics.firefly-iii.org/";
        _paq.push(['setTrackerUrl', u + 'matomo.php']);
        _paq.push(['setSiteId', '11']);
        var d = document, g = d.createElement('script'), s = d.getElementsByTagName('script')[0];
        g.async = true;
        g.src = u + 'matomo.js';
        s.parentNode.insertBefore(g, s);
    })();
</script>
<!-- End Matomo Code -->

</body>
</html>
